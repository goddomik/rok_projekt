<UserControl x:Class="rocnikový_projekt.Scena3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Viewbox Stretch="Fill">

        <Grid Width="800" Height="450">
            <Image Source="/imgs/scene3.png" Stretch="Fill"/>

            <Button Width="120"
                    Opacity="0.01"
                    Margin="351,86,329,251"
                    Click="KlikOkno"/>

            <Button
                    Opacity="0.01"
                    Margin="479,225,84,138"
                    Click="KlikPostel"/>

            <Button Width="100"
                    Opacity="0.01"
                    Margin="138,86,562,138"
                    Click="KlikSkrin"/>

            <Button
                    Opacity="0.01"
                    Margin="0,0,730,80"
                    Click="KlikPryc"/>

            <Border Background="#AA000000"
        Height="80"
        VerticalAlignment="Bottom">
                <TextBlock x:Name="TextPopis"
               Foreground="White"
               FontSize="16"
               Margin="10"/>
            </Border>
        </Grid>
    </Viewbox>
</UserControl>
